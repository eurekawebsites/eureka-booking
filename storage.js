export function createBookingStorage(business){const key=`eureka-booking:${business.id}:requests`;function read(){try{const v=JSON.parse(localStorage.getItem(key)||'[]');return Array.isArray(v)?v:[]}catch{return[]}}return{mode:'demo-local',async listRequests(){return read()},async saveRequest(request){const rows=read();rows.unshift(request);localStorage.setItem(key,JSON.stringify(rows.slice(0,100)));return request},async clearRequests(){localStorage.removeItem(key)}}}
/* Production boundary: replace with a client-owned backend adapter, authenticated admin operations and server-side slot collision protection. Do not put paid messaging providers into the public demo. */